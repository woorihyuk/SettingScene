using UnityEngine;

namespace Script.Settings
{
    public class ControlSettingData : SettingsData
    {
        // Start is called before the first frame update
        void Start()
        {
            Debug.Log("dfd");
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
